# 🔄 日常工作流程

本文档记录日常开发、预览、部署的完整流程。

---

## 🚀 每次开始工作前

### 1. 打开项目

```bash
# Mac
cd ~/Documents/GitHub/solomon0256.github.io

# Windows (PowerShell)
cd C:\Users\你的用户名\Documents\GitHub\solomon0256.github.io
```

### 2. 拉取最新代码（如果在多台电脑上工作）

```bash
git pull origin main
```

### 3. 启动本地服务器

```bash
bundle exec jekyll serve
```

服务器启动后，访问：**http://127.0.0.1:4000/**

> 💡 服务器会自动监听文件变化，保存文件后刷新浏览器即可看到更新

---

## ✏️ 常见修改任务

### 修改个人信息

1. 打开 `_data/profile.yml`
2. 修改对应字段
3. 保存文件，刷新浏览器查看效果

### 添加新项目

1. 在 `_data/profile.yml` 的 `recent_work` 部分添加新项目信息
2. 在 `projects/` 文件夹创建新的项目详情页 `.md` 文件
3. 保存并预览

### 添加新闻动态

1. 在 `_news/` 文件夹创建新文件，如 `2024-news-title.md`
2. 按照以下格式编写：

```markdown
---
title: "新闻标题"
date: 2024-12-19
---

新闻内容...
```

### 添加论文

1. 在 `_publications/` 对应年份文件夹创建 `.md` 文件
2. 参考现有文件的格式填写

### 更换头像

1. 将新头像图片重命名为 `portrait.jpg`
2. 替换 `assets/images/photos/portrait.jpg`
3. 建议尺寸：400x400 像素，正方形

---

## 📤 提交更改到 GitHub

### 1. 查看修改状态

```bash
git status
```

### 2. 添加所有更改

```bash
git add .
```

### 3. 提交更改

```bash
git commit -m "描述你的修改"
```

提交信息示例：
- `"Update personal info"`
- `"Add new project: xxx"`
- `"Fix typo in bio"`

### 4. 推送到 GitHub

```bash
git push origin main
```

### 5. 等待部署

- GitHub Pages 通常需要 1-2 分钟部署
- 访问 https://solomon0256.github.io 查看上线效果

---

## 🔄 完整工作流程示例

```bash
# 1. 进入项目目录
cd ~/Documents/GitHub/solomon0256.github.io

# 2. 拉取最新代码（多设备同步）
git pull origin main

# 3. 启动本地服务器
bundle exec jekyll serve

# 4. 打开浏览器预览
# 访问 http://127.0.0.1:4000/

# 5. 修改文件...

# 6. 查看修改
git status

# 7. 提交更改
git add .
git commit -m "你的修改描述"

# 8. 推送到 GitHub
git push origin main

# 9. 等待 1-2 分钟，查看线上效果
```

---

## 🛑 停止本地服务器

在终端按 `Ctrl + C` 停止服务器

---

## ⚠️ 常见问题

### 问题 1：本地预览和线上不一致

**原因**：缓存问题

**解决**：
- 浏览器强制刷新：`Cmd + Shift + R`（Mac）或 `Ctrl + Shift + R`（Windows）
- 清除浏览器缓存

### 问题 2：git push 被拒绝

**原因**：远程有新的提交

**解决**：

```bash
git pull origin main
# 解决可能的冲突
git push origin main
```

### 问题 3：Jekyll 构建失败

**解决**：

```bash
bundle exec jekyll serve --trace
```

查看详细错误信息

### 问题 4：端口被占用

**解决**：

```bash
# 使用其他端口
bundle exec jekyll serve --port 4001
```

---

## 📱 使用 VS Code 开发

### 推荐插件

1. **Markdown All in One** - Markdown 编辑增强
2. **Jekyll Snippets** - Jekyll 代码片段
3. **GitLens** - Git 增强
4. **Live Server** - 实时预览（可选）

### VS Code 终端

直接在 VS Code 中打开终端：
- Mac: `Cmd + ``
- Windows: `Ctrl + ``

---

## 🔁 多设备同步工作流

### 在设备 A 完成工作后

```bash
git add .
git commit -m "完成xxx工作"
git push origin main
```

### 切换到设备 B 开始工作

```bash
git pull origin main
# 继续工作...
```

---

*最后更新：2025年12月19日*

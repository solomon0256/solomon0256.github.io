# ⌨️ 常用命令速查表

本文档汇总开发过程中常用的命令，方便快速查阅。

---

## 📦 Jekyll 命令

| 命令 | 说明 |
|------|------|
| `bundle install` | 安装项目依赖 |
| `bundle update` | 更新依赖到最新版本 |
| `bundle exec jekyll serve` | 启动本地服务器（http://127.0.0.1:4000/） |
| `bundle exec jekyll serve --port 4001` | 使用其他端口启动 |
| `bundle exec jekyll serve --livereload` | 启动服务器并自动刷新 |
| `bundle exec jekyll build` | 构建静态文件到 `_site/` |
| `bundle exec jekyll clean` | 清理构建缓存 |

---

## 🔧 Git 基本命令

### 查看状态

```bash
git status          # 查看当前状态
git log --oneline   # 查看简洁提交历史
git diff            # 查看未暂存的更改
```

### 提交更改

```bash
git add .                        # 添加所有更改
git add <filename>               # 添加指定文件
git commit -m "提交信息"          # 提交更改
git commit -am "提交信息"         # 添加并提交（仅限已跟踪文件）
```

### 同步远程

```bash
git pull origin main             # 拉取远程更新
git push origin main             # 推送到远程
git fetch                        # 获取远程信息（不合并）
```

### 分支操作

```bash
git branch                       # 查看分支列表
git branch <name>                # 创建新分支
git checkout <branch>            # 切换分支
git checkout -b <name>           # 创建并切换分支
git merge <branch>               # 合并分支
```

### 撤销操作

```bash
git checkout -- <file>           # 撤销未暂存的更改
git reset HEAD <file>            # 取消暂存
git reset --hard HEAD            # 撤销所有未提交的更改（危险！）
git revert <commit>              # 撤销指定提交
```

---

## 💻 终端常用命令

### Mac / Linux

```bash
cd <path>                        # 进入目录
ls -la                           # 列出文件
pwd                              # 显示当前路径
mkdir <name>                     # 创建文件夹
rm <file>                        # 删除文件
rm -rf <folder>                  # 删除文件夹（危险！）
cp <src> <dest>                  # 复制文件
mv <src> <dest>                  # 移动/重命名文件
```

### Windows PowerShell

```powershell
cd <path>                        # 进入目录
dir                              # 列出文件
pwd                              # 显示当前路径
mkdir <name>                     # 创建文件夹
del <file>                       # 删除文件
Remove-Item -Recurse <folder>    # 删除文件夹
copy <src> <dest>                # 复制文件
move <src> <dest>                # 移动/重命名文件
```

---

## 🛠️ Ruby 相关命令

```bash
ruby -v                          # 查看 Ruby 版本
gem -v                           # 查看 gem 版本
gem list                         # 列出已安装的 gems
gem install <name>               # 安装 gem
gem uninstall <name>             # 卸载 gem
```

### rbenv（Mac）

```bash
rbenv versions                   # 列出所有已安装版本
rbenv global <version>           # 设置全局版本
rbenv local <version>            # 设置项目版本
rbenv install <version>          # 安装新版本
rbenv rehash                     # 刷新 shims
```

---

## 📋 常用操作组合

### 完整提交流程

```bash
git status                       # 1. 查看状态
git add .                        # 2. 添加更改
git commit -m "描述"              # 3. 提交
git push origin main             # 4. 推送
```

### 开始工作前

```bash
cd ~/Documents/GitHub/solomon0256.github.io
git pull origin main
bundle exec jekyll serve
```

### 解决冲突

```bash
git pull origin main             # 拉取并提示冲突
# 手动编辑冲突文件
git add .                        # 标记冲突已解决
git commit -m "Resolve conflicts"
git push origin main
```

### 查看远程仓库信息

```bash
git remote -v                    # 查看远程仓库地址
git remote show origin           # 查看远程仓库详情
```

---

## 🔍 调试技巧

### Jekyll 详细错误信息

```bash
bundle exec jekyll serve --trace
```

### 查看 Jekyll 配置

```bash
bundle exec jekyll doctor
```

### 查找端口占用（Mac）

```bash
lsof -i :4000
kill -9 <PID>
```

### 查找端口占用（Windows）

```powershell
netstat -ano | findstr :4000
taskkill /PID <PID> /F
```

---

## 📌 快捷键

### VS Code

| 快捷键 | Mac | Windows |
|--------|-----|---------|
| 打开终端 | `Cmd + `` | `Ctrl + `` |
| 保存文件 | `Cmd + S` | `Ctrl + S` |
| 全部保存 | `Cmd + Alt + S` | `Ctrl + K, S` |
| 搜索文件 | `Cmd + P` | `Ctrl + P` |
| 全局搜索 | `Cmd + Shift + F` | `Ctrl + Shift + F` |
| 侧边栏 | `Cmd + B` | `Ctrl + B` |

### 浏览器

| 快捷键 | Mac | Windows |
|--------|-----|---------|
| 强制刷新 | `Cmd + Shift + R` | `Ctrl + Shift + R` |
| 开发者工具 | `Cmd + Alt + I` | `Ctrl + Shift + I` |
| 清除缓存刷新 | `Cmd + Shift + R` | `Ctrl + F5` |

---

## 📚 有用链接

- Jekyll 文档: https://jekyllrb.com/docs/
- Liquid 模板语法: https://shopify.github.io/liquid/
- GitHub Pages 文档: https://docs.github.com/en/pages
- Bootstrap 文档: https://getbootstrap.com/docs/
- Font Awesome 图标: https://fontawesome.com/icons

---

## 🆘 遇到问题？

1. **先查看错误信息** - 运行 `bundle exec jekyll serve --trace`
2. **搜索错误信息** - 复制错误信息搜索 Google 或 Stack Overflow
3. **检查 Git 状态** - 运行 `git status` 确保没有未提交的冲突
4. **清理缓存重试** - 运行 `bundle exec jekyll clean` 后重新构建

---

更新日期：2025年12月19日

# 🛠️ 环境配置指南

本文档记录如何在 Mac 和 Windows 上配置 Jekyll 开发环境。

---

## 📌 当前环境信息（Mac）

在 Mac 上已经配置好的环境：

| 软件 | 版本 | 安装方式 |
|------|------|----------|
| Ruby | 3.2.2 | rbenv |
| Bundler | 2.4.10 | gem install |
| Jekyll | 3.10.0 | bundle install |

---

## 🍎 Mac 环境配置

### 1. 安装 Homebrew（如果没有）

```bash
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
```

### 2. 安装 rbenv（Ruby 版本管理器）

```bash
brew install rbenv ruby-build
```

### 3. 配置 rbenv

将以下内容添加到 `~/.zshrc`：

```bash
eval "$(rbenv init - zsh)"
```

然后重新加载：

```bash
source ~/.zshrc
```

### 4. 安装 Ruby

```bash
rbenv install 3.2.2
rbenv global 3.2.2
```

### 5. 安装 Bundler

```bash
gem install bundler
```

### 6. 安装项目依赖

```bash
cd /path/to/solomon0256.github.io
bundle install
```

---

## 🪟 Windows 环境配置

### 方法一：使用 RubyInstaller（推荐）

#### 1. 下载 RubyInstaller

前往 [RubyInstaller 官网](https://rubyinstaller.org/downloads/) 下载：
- **推荐版本**：Ruby+Devkit 3.2.x (x64)
- 选择带有 Devkit 的版本

#### 2. 安装 Ruby

1. 运行下载的安装程序
2. 勾选 "Add Ruby executables to your PATH"
3. 完成安装后，选择 "Run 'ridk install'" 安装 MSYS2

#### 3. 验证安装

打开 PowerShell 或 CMD：

```powershell
ruby -v
# 应显示 ruby 3.2.x

gem -v
# 应显示 gem 版本
```

#### 4. 安装 Bundler 和 Jekyll

```powershell
gem install bundler jekyll
```

#### 5. 克隆项目

```powershell
cd C:\Users\你的用户名\Documents\GitHub
git clone https://github.com/solomon0256/solomon0256.github.io.git
cd solomon0256.github.io
```

#### 6. 安装项目依赖

```powershell
bundle install
```

### 方法二：使用 WSL（Windows Subsystem for Linux）

如果你更习惯 Linux 环境，可以使用 WSL：

#### 1. 安装 WSL

以管理员身份运行 PowerShell：

```powershell
wsl --install
```

#### 2. 安装 Ubuntu

重启后，从 Microsoft Store 安装 Ubuntu。

#### 3. 在 WSL 中配置环境

按照上面 Mac 的步骤进行配置（将 Homebrew 替换为 apt）：

```bash
# 更新包管理器
sudo apt update && sudo apt upgrade

# 安装依赖
sudo apt install git curl autoconf bison build-essential libssl-dev libyaml-dev libreadline6-dev zlib1g-dev libncurses5-dev libffi-dev libgdbm6 libgdbm-dev libdb-dev

# 安装 rbenv
curl -fsSL https://github.com/rbenv/rbenv-installer/raw/HEAD/bin/rbenv-installer | bash

# 添加到 ~/.bashrc
echo 'export PATH="$HOME/.rbenv/bin:$PATH"' >> ~/.bashrc
echo 'eval "$(rbenv init -)"' >> ~/.bashrc
source ~/.bashrc

# 安装 Ruby
rbenv install 3.2.2
rbenv global 3.2.2

# 安装 Bundler
gem install bundler
```

---

## ⚠️ 常见问题

### 问题 1：bundle install 报错 SSL 证书问题

**解决方案**（Windows）：

```powershell
gem sources --add https://gems.ruby-china.com/ --remove https://rubygems.org/
bundle config mirror.https://rubygems.org https://gems.ruby-china.com
```

### 问题 2：Jekyll serve 启动失败

**解决方案**：

```bash
bundle exec jekyll serve --trace
```

查看详细错误信息。

### 问题 3：端口被占用

**解决方案**：

```bash
bundle exec jekyll serve --port 4001
```

### 问题 4：Windows 中文路径问题

建议将项目放在纯英文路径下，如：
- `C:\Projects\solomon0256.github.io`
- `C:\Users\YourName\GitHub\solomon0256.github.io`

---

## 📝 环境验证清单

配置完成后，运行以下命令确认环境正常：

```bash
# 检查 Ruby 版本
ruby -v
# 期望输出: ruby 3.2.x

# 检查 Bundler 版本  
bundler -v
# 期望输出: Bundler version 2.x.x

# 检查 Jekyll 版本
bundle exec jekyll -v
# 期望输出: jekyll 3.10.x

# 检查 Git 版本
git --version
# 期望输出: git version 2.x.x
```

---

*最后更新：2025年12月19日*
